def next_move():
    while True:
        move = input("Type Enter For Next Move!!: ")
        if move != "":
            print("Hit only Enter: ")
            continue
        else:
            break
